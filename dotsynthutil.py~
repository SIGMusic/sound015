import numpy as np

sr = 44100

def maketones(hd, dur, fund):
    t = np.arange(sr*dur)/sr.
    
    signal = np.zeros(sr*dur)

    for i in range(len(hd)):
        signal = signal + hd[i]*np.sin(2*np.pi*(i+1)*fund*t)
        
    return signal
